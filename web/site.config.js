/** اسم repository على GitHub — يجب أن يطابق اسم الـ repo بالضبط */
export const REPO_NAME = 'SULTAN124-ishak'

export const GITHUB_USER = 'Vostro213'

export const SITE_URL = `https://${GITHUB_USER}.github.io/${REPO_NAME}`

export const GITHUB_REPO = `https://github.com/${GITHUB_USER}/${REPO_NAME}`

export const BASE_PATH = `/${REPO_NAME}/`
